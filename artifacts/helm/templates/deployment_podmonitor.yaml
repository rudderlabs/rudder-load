# This is the pod monitor for the job ingestion producer
---
apiVersion: monitoring.coreos.com/v1
kind: PodMonitor
metadata:
  labels:
    app: dummy-load-scraper
    scrape-by: cluster-grafana-agent
  name: dummy-load-podmonitor
spec:
  podMetricsEndpoints:
  - port: metrics
  selector:
    matchLabels:
      run: {{ .Values.deployment.name }}
